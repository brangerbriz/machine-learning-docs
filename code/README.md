Install 

```bash
# activate the environment
source venv/bin/activate

# install the venv kernel for jupyter notebooks
python -m ipykernel install --user --name BB-ML-doc --display-name "Python (BB ML Doc)"
```
